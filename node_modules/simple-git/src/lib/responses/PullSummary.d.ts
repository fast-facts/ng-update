import { PullResult } from '../../../typings';
declare type PullResultFileChanges = {
    [fileName: string]: number;
};
declare type PullResultOverview = {
    changes: number;
    insertions: number;
    deletions: number;
};
export declare class PullSummary implements PullResult {
    created: string[];
    deleted: string[];
    files: string[];
    deletions: PullResultFileChanges;
    insertions: PullResultFileChanges;
    summary: PullResultOverview;
}
export declare function parsePull(text: string, pullSummary?: PullResult): PullResult;
export {};
