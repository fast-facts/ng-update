import { MergeConflict, MergeResult, MergeResultStatus } from '../../../typings';
import { PullSummary } from './PullSummary';
export declare class MergeSummaryResult extends PullSummary implements MergeResult {
    conflicts: MergeConflict[];
    merges: string[];
    result: MergeResultStatus;
    get failed(): boolean;
    get reason(): string;
    toString(): string;
}
export declare function parseMerge(text: string): MergeResult;
